{
    "type": "object",
    "description": "A Event-Participant object",
    "properties": {
      "id": {
        "type": "integer",
        "description": "The unique ID of the Event-Participant"
      },
      "participant_id": {
        "type": "integer",
        "description": "The ID of the Concert-Participant from 'ConcertParticipants' table"
      },
      "event_id": {
        "type": "integer",
        "description": "The ID of the Concert-Event from 'ConcertEvents' table"
      }
    },
    "required": ["participant_id", "event_id"]
  }
  